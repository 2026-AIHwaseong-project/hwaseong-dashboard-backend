15-165-83-80.sslip.io {
	# 업로드 본문 상한. 여기가 유일한 "받기 전" 방어선이다 — FastAPI 는 본문을
	# 전부 버퍼링한 뒤에야 핸들러를 부르므로, 애플리케이션 안의 크기 검사는
	# 이미 디스크·메모리를 다 쓴 뒤의 사후 통보다. 관리자 콘솔이 토큰 없이
	# 열려 있는 운영 전제라 이 한 줄이 실질적인 자원 고갈 방어가 된다.
	request_body {
		max_size 32MB
	}
	reverse_proxy api:8000
}
