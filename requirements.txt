# ================================================================
# 화성시 버스 대시보드 백엔드
# pip install -r requirements.txt
# python main.py          # 서버 기동
# ================================================================

# ── 데이터 파이프라인 (analysis/) ────────────────────────────────
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
requests>=2.31
python-dotenv>=1.0
openpyxl>=3.1
pyshp>=2.3
shapely>=2.0
pyproj>=3.6

# ── API 서버 (server/) ──────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.0

# ── AI 보고서 프록시 — 아래 중 하나 이상 키 설정 ─────────────────
# ANTHROPIC_API_KEY  → claude-sonnet-5 (기본)
anthropic>=0.28
# OPENAI_API_KEY     → gpt-5.6-sol
openai>=1.30
# GOOGLE_API_KEY     → gemini-3.1-pro-preview
google-generativeai>=0.8

# ── PostgreSQL 적재 (선택: 06_load.py --db) ──────────────────────
# psycopg2-binary>=2.9
